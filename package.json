{
  "name": "foodnouns-root",
  "private": true,
  "engines": {
    "node": ">=16.20.0"
  },
  "scripts": {
    "lerna": "lerna",
    "build": "lerna run build",
    "test": "lerna run --parallel test",
    "bootstrap": "yarn cache clean && rm -rf node_modules && yarn install && lerna bootstrap",
    "clean": "lerna run clean --scope @foodnouns/* && rm -rf coverage test-report node_modules && yarn cache clean",
    "prepare": "lerna run prepare --scope=@foodnouns/{assets,contracts,sdk}",
    "dev:web": "cd packages/foodnouns-webapp && yarn start",
    "lint": "eslint 'packages/**/*.ts' --fix",
    "format": "prettier --write 'packages/**/*.{ts(x)?,sol,md,css,json}' '!**/typechain/**'"
  },
  "devDependencies": {
    "@types/chai": "^4.2.15",
    "@types/mocha": "^8.2.2",
    "@types/node": "^14.14.34",
    "@typescript-eslint/eslint-plugin": "^6.7.3",
    "@typescript-eslint/parser": "^6.7.3",
    "chai": "^4.2.0",
    "eslint": "^7.28.0",
    "eslint-config-prettier": "^8.3.0",
    "husky": "^5.0.9",
    "lerna": "5.2.0",
    "mocha": "^8.4.0",
    "pinst": "^2.1.6",
    "prettier": "^2.3.0",
    "rimraf": "^3.0.2",
    "ts-node": "^10.0.0",
    "typescript": "^4.7.4"
  },
  "workspaces": [
    "packages/*"
  ],
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{ts,js}": [
      "yarn lint",
      "yarn format"
    ]
  }
}
